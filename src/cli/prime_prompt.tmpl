# ish Agent Guide

Use `ish` as the source of truth for work in {{project_name}}.

## Project Defaults

- Workspace path: `{{config_path}}`
- Prefix new IDs with `{{prefix}}-`
- Default new ish type: `{{default_type}}`
- Default new ish status: `{{default_status}}`

## CLI Command Reference

{{commands}}

Use `ish list --json` for machine-readable output. Prefer structured output whenever a command supports it so agents can parse results instead of scraping terminal text.

## Ish Types

{{types}}

## Ish Statuses

{{statuses}}

## Ish Priorities

{{priorities}}

## Body Modification Guide

- Treat the body as markdown content stored after the YAML frontmatter.
- Prefer exact-match body replacements when updating checklists or headings so concurrent edits are easier to validate.
- Append new sections instead of rewriting the whole body when you only need to add notes, verification details, or a summary of changes.
- Keep checklist items accurate as work progresses; mark each completed item with `- [x]` as soon as it is done.

## Concurrency Control

- Read the latest ish content before editing it.
- Use `ish update --if-match <etag>` when protecting an in-place edit against concurrent changes.
- Capture the current etag from `ish list --json --full` before attempting a guarded update.
- If the underlying file changes between read and write, reload the ish and reapply the intended edit against the newest content.

## Relationship Model

- `parent`: hierarchy only; use it to group related ishes under milestones, epics, or larger features.
- `blocking`: IDs of ishes that this ish is actively blocking.
- `blocked_by`: IDs of prerequisites that must finish before this ish can move forward.
- Do not infer blocking from parent/child relationships alone; hierarchy and dependency links mean different things.

## Working Rules For Agents

- Prefer small, focused edits that keep the existing structure intact.
- Record useful implementation notes, verification details, and follow-up context in the relevant ish before marking it complete.
- Run the project validation loop after changes and fix failures before considering the work done.
